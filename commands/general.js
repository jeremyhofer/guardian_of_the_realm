module.exports = {
  help (args, client, msg) {
    // Show help text. optional specific command
  },
  bal (args, client, msg) {
    // Lists the players money, men, and ships with the unique faction name for each.
    msg.reply("Our finest artificers are working night and day; we apologize for not being able to find your records.");
    return 0;
  }
};